using PetEmulator.Cpc;

namespace PetEmulator.Cpc464;

/// <summary>CPC464 compatibility type; the matrix implementation is shared by all CPC models.</summary>
public sealed class Cpc464Keyboard : CpcKeyboard;
