<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <RootNamespace>PetEmulator.Vic20</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../PetEmulator.Core/PetEmulator.Core.csproj" />
    <ProjectReference Include="../PetEmulator.Cpu6502/PetEmulator.Cpu6502.csproj" />
    <!-- Via6522 (PetEmulator.Pet.Chips) has zero PET-specific dependencies - a plain MOS 6522
         chip class built on PetEmulator.Core alone. VIC-20 has two VIAs on real hardware; reusing
         this one directly avoids a ~250-line duplicate (see docs/vic20-migration-plan.md step 4). -->
    <ProjectReference Include="../PetEmulator.Pet/PetEmulator.Pet.csproj" />
  </ItemGroup>
</Project>
